module.exports = class RulesEngine {
  constructor(rules, configs) {
    this.rules = rules;
    this.configs = configs;
  }

  received(configs, data) {
  }

  slope(data) {
  }

  expectedCompletion(data) {
  }

};
